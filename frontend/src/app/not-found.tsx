import { NotFoundState } from "@/components/shared/states/not-found-state";

export default function NotFound() {
  return (
    <div className="flex-1 flex items-center justify-center p-6 min-h-[70vh]">
      <NotFoundState />
    </div>
  );
}
